<div class="search_result">
  <h2><%= @room_number %>教室 利用状況</h2>
    <div class="room_timetable">
        <table class="room_timetable-table">
            <caption>
                〇・・・空き<br>
                ×・・・講義中
            </caption>
            <tr>
                <% ["", "月", "火", "水", "木", "金", "土", "日"].each do |day| %>
                    <th><%= day %></th>
                <% end %>
            </tr>
            <% [1, 2, 3, 4, 5].each do |time| %>
                <tr>
                    <td><%= time %></td>
                    <% ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"].each do |day| %>
                        <% if @room_data.any? {|row| row[:day] == day && row[:time] == time} %>
                            <td>×</td>
                        <% else %>
                            <td>〇</td>
                        <% end %>
                    <% end %>
                </tr>
            <% end %>
        </table>
    </div>
    <div class="search_result_desc">
        <p>
         <%= @room_number %> 教室の利用状況を表示しています。<br>
        休講や補講、学内イベント等により、実際の利用状況が表示内容と異なる場合があります。
        </p>
    </div>
</div>